fix(api): Add null check for art style in illustration generation

This prevents a TypeError that occurs when attempting to generate an image without providing an art style reference. The code was trying to access a property on a null object, causing a 500 internal server error.